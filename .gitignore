# Dependencies
node_modules/
pnpm-lock.yaml
yarn.lock
# Dependencies
node_modules/
# package-lock.json is usually verified, keeping it commented out so it IS committed by default
# package-lock.json

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
.next/
out/

# IDEs
.vscode/
.idea/

# Backend uploads (keep gitkeep but ignore files)
server/uploads/*
!server/uploads/.gitkeep
# If you don't have a .gitkeep, just ignore the folder content or folder itself if ephemeral
# Assuming we want to keep the folder structure
